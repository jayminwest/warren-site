---
order: 6
eyebrow: "Judge"
glyph: "scale"
title: "A verdict on every finished run"
---

An isolated judge scores each finished run against a 15-class rubric and serves the verdicts at `GET /verdicts.jsonl`. Agents never see their verdicts, so the agents cannot game the metric.
